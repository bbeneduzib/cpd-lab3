#include <bits/stdc++.h>
#include <fstream>

#ifndef utils_h
#define utils_h

using namespace std;

void CountOccurrences(vector<string> *arr, int size, ofstream &output_stream);

bool IsLetters(string input);

#endif
